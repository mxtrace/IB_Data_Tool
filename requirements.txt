# IB Data Tool v2.0.0 — Python dependencies
# Install: pip install -r requirements.txt

# Core (API data fetch)
requests>=2.28
urllib3>=2.0
openpyxl>=3.1

# Excel .xls support (CDA/ASI files)
xlrd>=2.0

# Outlook COM + Windows API
pywin32>=306

# Browser automation (only for CDA/ASI download fallback)
pyautogui>=0.9
